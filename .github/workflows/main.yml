on:
  push:
    branches:
      - main
jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Hello, World!"
      - name: Step 2
        run: echo "Workflow is running."
